<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bryan Robertson - Visual Artist</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Helvetica', 'Arial', sans-serif;
            background-color: #f5f5f5;
        }

        .parallax-section {
            height: 100vh;
            overflow: hidden;
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .floating-image {
            position: absolute;
            width: 400px;
            height: 300px;
            object-fit: cover;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
            animation: floatRight 20s linear infinite;
        }

        @keyframes floatRight {
            from {
                transform: translateX(-100%);
            }
            to {
                transform: translateX(100vw);
            }
        }

        header {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: rgba(255, 255, 255, 0.9);
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: #333;
            text-decoration: none;
        }

        .logo img {
            height: 50px;
            width: auto;
        }

        nav {
            display: flex;
            gap: 1rem;
        }

        nav a {
            text-decoration: none;
            color: white;
            background-color: #20B2AA;
            padding: 0.8rem 1.2rem;
            border-radius: 25px;
            transition: all 0.3s ease;
            font-family: 'Helvetica', 'Arial', sans-serif;
        }

        nav a:hover {
            background-color: #178F89;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }

        .section-content {
            position: relative;
            z-index: 1;
            color: #333;
            text-align: center;
        }

        .section-content h1 {
            font-size: 4rem;
            margin-bottom: 1rem;
            font-weight: 300;
        }

        .section-content p {
            font-size: 1.5rem;
            font-weight: 300;
        }

        @media (max-width: 768px) {
            header {
                flex-direction: column;
                padding: 1rem;
            }

            nav {
                margin-top: 1rem;
            }

            .section-content h1 {
                font-size: 2.5rem;
            }

            .section-content p {
                font-size: 1.2rem;
            }

            .floating-image {
                width: 300px;
                height: 225px;
            }
        }
    </style>
</head>
<body>
    <header>
        <a href="#" class="logo">
            <img src="images/logo.png" alt="Bryan Robertson Logo">
        </a>
        <nav>
            <a href="#painting">Painting and Drawing</a>
            <a href="#digital">Digital Art</a>
            <a href="#about">About</a>
        </nav>
    </header>

    <section class="parallax-section">
        <img src="bellyfeel.gif" class="floating-image" style="top: 20%; animation-delay: 0s;" alt="Artwork 1">
        <img src="images/artwork2.jpg" class="floating-image" style="top: 40%; animation-delay: -5s;" alt="Artwork 2">
        <img src="images/artwork3.jpg" class="floating-image" style="top: 60%; animation-delay: -10s;" alt="Artwork 3">
        <div class="section-content">
            <h1>Bryan Robertson</h1>
            <p>Visual Artist | Creator | Innovator</p>
        </div>
    </section>

</body>
</html>