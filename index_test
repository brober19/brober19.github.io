<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bryan Robertson - Visual Artist</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
        }

        .parallax-section {
            height: 100vh;
            overflow: hidden;
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .parallax-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 120%; /* Extra height for parallax effect */
            background-size: cover;
            background-position: center;
            transform: translateZ(0);
            will-change: transform;
        }

        header {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: rgba(255, 255, 255, 0.9);
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: #333;
            text-decoration: none;
        }

        .logo img {
            height: 50px;
            width: auto;
        }

        nav {
            display: flex;
            gap: 1rem;
        }

        nav a {
            text-decoration: none;
            color: white;
            background-color: #20B2AA;
            padding: 0.8rem 1.2rem;
            border-radius: 25px;
            transition: all 0.3s ease;
        }

        nav a:hover {
            background-color: #178F89;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }

        .section-content {
            position: relative;
            z-index: 1;
            color: white;
            text-align: center;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }

        .section-content h1 {
            font-size: 4rem;
            margin-bottom: 1rem;
        }

        .section-content p {
            font-size: 1.5rem;
        }

        @media (max-width: 768px) {
            header {
                flex-direction: column;
                padding: 1rem;
            }

            nav {
                margin-top: 1rem;
            }

            .section-content h1 {
                font-size: 2.5rem;
            }

            .section-content p {
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <a href="#" class="logo">
            <img src="path/to/your/logo.png" alt="Bryan Robertson Logo">
        </a>
        <nav>
            <a href="#painting">Painting and Drawing</a>
            <a href="#digital">Digital Art</a>
            <a href="#about">About</a>
        </nav>
    </header>

    <section class="parallax-section">
        <div class="parallax-bg" style="background-image: url('path/to/your/image1.jpg');"></div>
        <div class="section-content">
            <h1>Bryan Robertson</h1>
            <p>Visual Artist | Creator | Innovator</p>
        </div>
    </section>

    <section class="parallax-section">
        <div class="parallax-bg" style="background-image: url('path/to/your/image2.jpg');"></div>
        <div class="section-content">
            <h1>Painting & Drawing</h1>
            <p>Traditional Media Works</p>
        </div>
    </section>

    <section class="parallax-section">
        <div class="parallax-bg" style="background-image: src="bellyfeel.gif;"></div>
        <div class="section-content">
            <h1>Digital Art</h1>
            <p>Digital Creations</p>
        </div>
    </section>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            window.addEventListener('scroll', function() {
                const parallaxSections = document.querySelectorAll('.parallax-section');

                parallaxSections.forEach(section => {
                    const distance = window.pageYOffset - section.offsetTop;
                    const parallaxBg = section.querySelector('.parallax-bg');

                    if (Math.abs(distance) < window.innerHeight) {
                        parallaxBg.style.transform = `translateY(${distance * 0.5}px)`;
                    }
                });
            });
        });
    </script>
</body>
</html>